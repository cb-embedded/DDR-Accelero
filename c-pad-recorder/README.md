# Pad Recorder

DirectInput gamepad recorder for Windows.

## Compile

```bash
mkdir build
cd build
cmake .. -G "MinGW Makefiles"
cmake --build .
```

## Run

```bash
.\build\pad_recorder_dinput.exe
```

Press Ctrl+C to stop recording.
